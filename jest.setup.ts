/*
  Loaded before every test file (see jest.config.ts).

  `@testing-library/jest-dom` adds the DOM matchers — toBeInTheDocument,
  toBeDisabled, toHaveValue and friends — which read far better in a failure
  message than poking at properties by hand.

  Harmless in the node-environment backend suites: it only registers matchers.
*/
import "@testing-library/jest-dom";
